# Decisions

Append-only. Every judgment call made without the user, newest at the bottom.
Mark with ⚠ anything you want reviewed in the morning.

| # | WP | Decision | Alternative rejected | Why | Reversible? | Grade |
|---|----|----------|---------------------|-----|-------------|-------|

`Grade` is the user's, filled in over coffee: `good` / `bad` / `bad: <what I'd have done>` / blank.
`/df-retro` reads that column to decide what the harness learns from this project — a graded bad call
is the single highest-signal input `LESSONS.md` ever gets.
