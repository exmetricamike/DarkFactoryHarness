---
description: Audit and trim a project's always-loaded CLAUDE.md/AGENTS.md context to lower per-turn cost
---

Invoke the `optimize-scaffolding` skill.

Target directory: `$ARGUMENTS` (if empty, use the current working directory).

Follow the skill's steps in order and stop for explicit approval before writing any file.
